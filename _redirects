# Cloudflare Pages Redirects (Netlify Format)
# SPA Configuration - All routes serve index.html

# Static assets should be served directly (no redirect)
/css/*    /css/:splat    200
/js/*     /js/:splat     200
/assets/* /assets/:splat 200

# Specific HTML pages (if they exist)
/impressum    /impressum.html    200
/datenschutz  /datenschutz.html  200
/licenses     /licenses.html     200

# Robots and sitemap
/robots.txt   /robots.txt   200
/sitemap.xml  /sitemap.xml  200

# SPA Fallback - All other routes go to index.html
# This enables client-side routing for /convert/* URLs
/*  /index.html  200
